spring:
  application:
    name: auth-service

  logging:
    level:
      org.springframework.security: DEBUG

  datasource:
    url: jdbc:mysql://localhost:3306/authsecuritydemo
    username: root
    password: Radhe123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


